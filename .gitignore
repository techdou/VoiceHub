# ── 构建产物 ──────────────────────────────
target/
node_modules/
dist/
# 安装包走 GitHub Releases，不进版本库
artifacts/

# ── 密钥与本地环境（私密信息不泄露）────────
.env
.env.*
*.local

# ── 开发软件配置目录（不上传）──────────────
.vscode/
.idea/
*.code-workspace

# ── 内部文档（不纳入版本控制）──────────────
docs/
PROJECT_PLAN.md

# ── 第三方参考克隆（仅本地阅读）────────────
refs/
remote-mic-app-windows/

# ── Tauri 生成物 ──────────────────────────
src-tauri/gen/schemas/

# ── 系统与临时文件 ─────────────────────────
.DS_Store
Thumbs.db
*.log
*.tmp
*.bak
